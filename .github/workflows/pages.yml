name: Docs
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upload docs (placeholder)
        run: echo "Render docs locally or with Quarto action if desired."
